/*
const rowsDataPDF =  async (obj, li) => {
    try {
        let listRows = [];
        for (let rom of li) {
            obj[rom].forEach(element => {
                element["romaneio"] = rom;
                listRows.push(element);                      
            });
        };
        listRows.pop()
        return listRows; 
    } catch (err) {
        console.log(`Houve um erro ao carregar algumas informações: ${err}`);
        return {};
    };
};
*/